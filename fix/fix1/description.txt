This fix addresses issues where our program was not ignoring input after ***. Both 89 and 90 are correctness violations that deal with the server not ignoring input after ***. We now truncate the program and accept a valid program if it is valid up to the point that the terminator is inserted.